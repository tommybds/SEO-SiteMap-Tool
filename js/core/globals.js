// Auto-split from app.js
    var form = document.getElementById('audit-form');
    var runBtn = document.getElementById('run-btn');
    var formError = document.getElementById('form-error');
    var modeSitemapBtn = document.getElementById('mode-sitemap-btn');
    var modeMeshBtn = document.getElementById('mode-mesh-btn');
    var modeTechBtn = document.getElementById('mode-tech-btn');
    var modeRedirectBtn = document.getElementById('mode-redirect-btn');
    var modeAccessibilityBtn = document.getElementById('mode-accessibility-btn');
    var modeGeoBtn = document.getElementById('mode-geo-btn');
    var catSiteBtn = document.getElementById('cat-site-btn');
    var catPageBtn = document.getElementById('cat-page-btn');
    var modesSiteGroup = document.getElementById('modes-site');
    var modesPageGroup = document.getElementById('modes-page');
    var MODE_TO_CATEGORY = {
      sitemap: 'site',
      mesh: 'site',
      tech: 'page',
      security: 'page',
      accessibility: 'page',
      images: 'page',
      geo: 'page',
      redirect: 'page',
    };
    var CATEGORY_DEFAULT_MODE = {
      site: 'sitemap',
      page: 'tech',
    };
    var modeHelp = document.getElementById('mode-help');
    var sitemapModePanel = document.getElementById('sitemap-mode-panel');
    var meshModePanel = document.getElementById('mesh-mode-panel');
    var techModePanel = document.getElementById('tech-mode-panel');
    var redirectModePanel = document.getElementById('redirect-mode-panel');
    var accessibilityModePanel = document.getElementById('accessibility-mode-panel');
    var geoModePanel = document.getElementById('geo-mode-panel');
    var meshForm = document.getElementById('mesh-form');
    var meshRunBtn = document.getElementById('mesh-run-btn');
    var meshFormError = document.getElementById('mesh-form-error');
    var meshStartUrl = document.getElementById('mesh_start_url');
    var meshMaxPages = document.getElementById('mesh_max_pages');
    var meshTimeout = document.getElementById('mesh_timeout');
    var techForm = document.getElementById('tech-form');
    var techRunBtn = document.getElementById('tech-run-btn');
    var techFormError = document.getElementById('tech-form-error');
    var techUrlInput = document.getElementById('tech_url');
    var techTimeoutInput = document.getElementById('tech_timeout');
    var redirectForm = document.getElementById('redirect-form');
    var redirectRunBtn = document.getElementById('redirect-run-btn');
    var redirectFormError = document.getElementById('redirect-form-error');
    var redirectUrlInput = document.getElementById('redirect_url');
    var redirectTimeoutInput = document.getElementById('redirect_timeout');
    var accessibilityForm = document.getElementById('accessibility-form');
    var accessibilityRunBtn = document.getElementById('accessibility-run-btn');
    var accessibilityFormError = document.getElementById('accessibility-form-error');
    var accessibilityUrlInput = document.getElementById('accessibility_url');
    var accessibilityStandardInput = document.getElementById('accessibility_standard');
    var accessibilityTimeoutInput = document.getElementById('accessibility_timeout');
    var modeSecurityBtn = document.getElementById('mode-security-btn');
    var securityModePanel = document.getElementById('security-mode-panel');
    var securityForm = document.getElementById('security-form');
    var securityRunBtn = document.getElementById('security-run-btn');
    var securityFormError = document.getElementById('security-form-error');
    var securityUrlInput = document.getElementById('security_url');
    var securityTimeoutInput = document.getElementById('security_timeout');
    var securityCard = document.getElementById('security-card');
    var securityStatusBox = document.getElementById('security-status-box');
    var securitySectionTitle = document.getElementById('security-section-title');
    var securityKpis = document.getElementById('security-kpis');
    var securityChecks = document.getElementById('security-checks');
    var securityRecos = document.getElementById('security-recos');
    var labelSecurityUrl = document.getElementById('label-security-url');
    var labelSecurityTimeout = document.getElementById('label-security-timeout');
    var geoForm = document.getElementById('geo-form');
    var geoRunBtn = document.getElementById('geo-run-btn');
    var geoFormError = document.getElementById('geo-form-error');
    var geoUrlInput = document.getElementById('geo_url');
    var geoTimeoutInput = document.getElementById('geo_timeout');
    var resultCard = document.getElementById('result-card');
    var meshCard = document.getElementById('mesh-card');
    var techCard = document.getElementById('tech-card');
    var redirectCard = document.getElementById('redirect-card');
    var accessibilityCard = document.getElementById('accessibility-card');
    var geoCard = document.getElementById('geo-card');
    var actionPlanCard = document.getElementById('action-plan-card');
    var actionPlanToggle = document.getElementById('action-plan-toggle');
    var actionPlanBody = document.getElementById('action-plan-body');
    var meshSectionTitle = document.getElementById('mesh-section-title');
    var techSectionTitle = document.getElementById('tech-section-title');
    var redirectSectionTitle = document.getElementById('redirect-section-title');
    var accessibilitySectionTitle = document.getElementById('accessibility-section-title');
    var geoSectionTitle = document.getElementById('geo-section-title');
    var actionPlanSectionTitle = document.getElementById('action-plan-section-title');
    var actionPlanStatus = document.getElementById('action-plan-status');
    var actionPlanKpis = document.getElementById('action-plan-kpis');
    var actionPlanNow = document.getElementById('action-plan-now');
    var actionPlanOwners = document.getElementById('action-plan-owners');
    var actionPlanSources = document.getElementById('action-plan-sources');
    var actionPlanBacklog = document.getElementById('action-plan-backlog');
    var redirectFlow = document.getElementById('redirect-flow');
    var meshStatusBox = document.getElementById('mesh-status-box');
    var techStatusBox = document.getElementById('tech-status-box');
    var redirectStatusBox = document.getElementById('redirect-status-box');
    var accessibilityStatusBox = document.getElementById('accessibility-status-box');
    var geoStatusBox = document.getElementById('geo-status-box');
    var redirectTechDetailsSummary = document.getElementById('redirect-tech-details-summary');
    var meshInteractionHint = document.getElementById('mesh-interaction-hint');
    var shareMeshBtn = document.getElementById('share-mesh-btn');
    var shareMeshFeedback = document.getElementById('share-mesh-feedback');
    var meshToggleGraphBtn = document.getElementById('mesh-toggle-graph-btn');
    var meshResetFocusBtn = document.getElementById('mesh-reset-focus-btn');
    var meshResetViewBtn = document.getElementById('mesh-reset-view-btn');
    var meshGraph = document.getElementById('mesh-graph');
    var meshHoverTooltip = document.getElementById('mesh-hover-tooltip');
    var meshMeta = document.getElementById('mesh-meta');
    var meshKpis = document.getElementById('mesh-kpis');
    var meshWarnings = document.getElementById('mesh-warnings');
    var meshActions = document.getElementById('mesh-actions');
    var meshOpportunities = document.getElementById('mesh-opportunities');
    var meshSummary = document.getElementById('mesh-summary');
    var meshErrors = document.getElementById('mesh-errors');
    var techKpis = document.getElementById('tech-kpis');
    var techChecks = document.getElementById('tech-checks');
    var techRecos = document.getElementById('tech-recos');
    var redirectKpis = document.getElementById('redirect-kpis');
    var redirectChecks = document.getElementById('redirect-checks');
    var redirectRecos = document.getElementById('redirect-recos');
    var accessibilityKpis = document.getElementById('accessibility-kpis');
    var accessibilityChecks = document.getElementById('accessibility-checks');
    var accessibilityRecos = document.getElementById('accessibility-recos');
    var geoKpis = document.getElementById('geo-kpis');
    var geoChecks = document.getElementById('geo-checks');
    var geoRecos = document.getElementById('geo-recos');
    var statusBox = document.getElementById('status-box');
    var logTail = document.getElementById('log-tail');
    var runError = document.getElementById('run-error');
    var downloadLink = document.getElementById('download-link');
    var conflictsDownloadLink = document.getElementById('conflicts-download-link');
    var shareReportBtn = document.getElementById('share-report-btn');
    var shareFeedback = document.getElementById('share-feedback');
    var summary = document.getElementById('summary');
    var kpiTotal = document.getElementById('kpi-total');
    var kpiOk = document.getElementById('kpi-ok');
    var kpiKo = document.getElementById('kpi-ko');
    var insightsBox = document.getElementById('insights-box');
    var insightsList = document.getElementById('insights-list');
    var topIssuesBox = document.getElementById('top-issues-box');
    var domainOverviewBox = document.getElementById('domain-overview-box');
    var previewCard = document.getElementById('preview-card');
    var previewMeta = document.getElementById('preview-meta');
    var previewError = document.getElementById('preview-error');
    var tableWrap = document.getElementById('table-wrap');
    var diffBox = document.getElementById('diff-box');
    var historyBox = document.getElementById('history-box');
    var filterIssue = document.getElementById('filter-issue');
    var filterStatus = document.getElementById('filter-status');
    var filterIndexable = document.getElementById('filter-indexable');
    var filterPriority = document.getElementById('filter-priority');
    var resetFiltersBtn = document.getElementById('reset-filters-btn');
    var langFrBtn = document.getElementById('lang-fr-btn');
    var langEnBtn = document.getElementById('lang-en-btn');
    var metaDescriptionTag = document.getElementById('meta-description');
    var appTitle = document.getElementById('app-title');
    var appSubtitle = document.getElementById('app-subtitle');
    var labelSitemap = document.getElementById('label-sitemap');
    var labelMaxUrls = document.getElementById('label-max-urls');
    var labelWorkers = document.getElementById('label-workers');
    var labelTimeout = document.getElementById('label-timeout');
    var labelSkipRobotsText = document.getElementById('label-skip-robots-text');
    var labelMeshStartUrl = document.getElementById('label-mesh-start-url');
    var labelMeshMaxPages = document.getElementById('label-mesh-max-pages');
    var labelMeshTimeout = document.getElementById('label-mesh-timeout');
    var labelTechUrl = document.getElementById('label-tech-url');
    var labelTechTimeout = document.getElementById('label-tech-timeout');
    var labelRedirectUrl = document.getElementById('label-redirect-url');
    var labelRedirectTimeout = document.getElementById('label-redirect-timeout');
    var labelAccessibilityUrl = document.getElementById('label-accessibility-url');
    var labelAccessibilityStandard = document.getElementById('label-accessibility-standard');
    var labelAccessibilityTimeout = document.getElementById('label-accessibility-timeout');
    var labelGeoUrl = document.getElementById('label-geo-url');
    var labelGeoTimeout = document.getElementById('label-geo-timeout');
    var jobSectionTitle = document.getElementById('job-section-title');
    var kpiTotalLabel = document.getElementById('kpi-total-label');
    var kpiOkLabel = document.getElementById('kpi-ok-label');
    var kpiKoLabel = document.getElementById('kpi-ko-label');
    var insightsTitle = document.getElementById('insights-title');
    var logsSummary = document.getElementById('logs-summary');
    var previewTitle = document.getElementById('preview-title');
    var filterIssueLabel = document.getElementById('filter-issue-label');
    var filterHttpLabel = document.getElementById('filter-http-label');
    var filterIndexableLabel = document.getElementById('filter-indexable-label');
    var filterPriorityLabel = document.getElementById('filter-priority-label');
    var footerCreditPrefix = document.getElementById('footer-credit-prefix');
    var hasTechMode = Boolean(modeTechBtn && techModePanel && techForm && techRunBtn && techCard);
    var hasRedirectMode = Boolean(modeRedirectBtn && redirectModePanel && redirectForm && redirectRunBtn && redirectCard);
    var hasAccessibilityMode = Boolean(modeAccessibilityBtn && accessibilityModePanel && accessibilityForm && accessibilityRunBtn && accessibilityCard);
    var hasGeoMode = Boolean(modeGeoBtn && geoModePanel && geoForm && geoRunBtn && geoCard);
    var hasSecurityMode = Boolean(modeSecurityBtn && securityModePanel && securityForm && securityRunBtn && securityCard);
    var modeImagesBtn = document.getElementById('mode-images-btn');
    var imagesModePanel = document.getElementById('images-mode-panel');
    var imagesForm = document.getElementById('images-form');
    var imagesRunBtn = document.getElementById('images-run-btn');
    var imagesFormError = document.getElementById('images-form-error');
    var imagesUrlInput = document.getElementById('images_url');
    var imagesTimeoutInput = document.getElementById('images_timeout');
    var imagesCard = document.getElementById('images-card');
    var imagesStatusBox = document.getElementById('images-status-box');
    var imagesSectionTitle = document.getElementById('images-section-title');
    var imagesKpis = document.getElementById('images-kpis');
    var imagesChecks = document.getElementById('images-checks');
    var imagesRecos = document.getElementById('images-recos');
    var labelImagesUrl = document.getElementById('label-images-url');
    var labelImagesTimeout = document.getElementById('label-images-timeout');
    var hasImagesMode = Boolean(modeImagesBtn && imagesModePanel && imagesForm && imagesRunBtn && imagesCard);

    var pollTimer = null;
    var meshPollTimer = null;
    var previewLoadedFor = null;
    var previewDataset = [];
    var previewHeaders = [];
    var previewTotalRows = 0;
    var previewTruncated = false;
    var currentSort = { key: 'priority_score', direction: 'desc' };
    var latestStatusPayload = null;
    var currentLang = 'fr';
    var currentMode = 'tech';
    var sitemapHasOutput = false;
    var latestMeshPayload = null;
    var latestTechPayload = null;
    var latestRedirectPayload = null;
    var latestAccessibilityPayload = null;
    var latestGeoPayload = null;
    var latestSecurityPayload = null;
    var latestImagesPayload = null;
    var latestActionPlanPayload = null;
    var meshGraphController = null;
    var meshClearSelection = null;
    var meshResetView = null;
    var meshOpportunityState = {
      sortKey: 'impact_score',
      direction: 'desc',
      quickWinsOnly: false,
      contextualOnly: false,
      minConfidence: 0,
    };
    var meshGraphVisible = false;

    var I18N = window.SEO_TOOL_I18N || { fr: {}, en: {} };

    
